Features
    Updates
    microcontroller flashing (maybe dead)
    gcode console

Unwanted Features
    pause_resume extrusion
    z_adjust before first move crashes
    timer to close error (on live move, homing, connect)
    DripModeEndSignal during homing due to add. gcode move command or stop command
    Keyboard automatically closes when typing too fast
    
    handle absense of xml material properties if its not guaranteed they exist 
